import React, { Component, Provider } from "react";
import { View, Text } from "react-native";
import AppContainer from "./DrawerNavigation";

const Profiles = () => {
  return <AppContainer />;
};

export default Profiles;
